# مخطط مشروع: شطرنج لثلاثة لاعبين (Web / HTML)

> هذه وثيقة تصميم (Specification) تُسلَّم للمطوّر (Sonnet) لتنفيذ اللعبة.
> الهدف: لعبة شطرنج تُلعَب بثلاثة لاعبين في آنٍ واحد عبر واجهة ويب (HTML/CSS/JS).

---

## 1. نظرة عامة على المشروع

- **النوع:** لعبة شطرنج بثلاثة لاعبين على لوحة سداسية (Three-player hexagonal chessboard).
- **المنصّة:** صفحة ويب تعمل في المتصفح، دون الحاجة لخادم (واجهة أمامية فقط في النسخة الأولى).
- **التقنيات المطلوبة:**
  - `HTML5` لهيكل الصفحة.
  - `CSS3` للتنسيق والتصميم (يفضَّل استخدام SVG أو Canvas لرسم اللوحة).
  - `JavaScript` (Vanilla بدون أُطر عمل في النسخة الأولى) لمنطق اللعبة.
- **بدون مكتبات خارجية إلزامية**، يمكن استخدام SVG مباشرة لرسم اللوحة السداسية.

---

## 2. قواعد اللعبة (Three-player Chess)

### 2.1 اللوحة
- لوحة على شكل **سداسي منتظم** مقسّمة إلى **96 خانة** (بدلًا من 64 في الشطرنج العادي).
- تتكوّن من ثلاثة قطاعات، كل قطاع يخصّ لاعبًا وهو امتداد لرقعة 8×8 المعتادة.
- الخانات ملوّنة بثلاثة ألوان متناوبة (فاتح / متوسط / غامق) للحفاظ على نمط التبادل.
- كل لاعب لديه **16 قطعة** بنفس ترتيب الشطرنج الكلاسيكي (8 بيادق، رخّان، حصانان، فيلان، وزير، ملك).

### 2.2 اللاعبون
- ثلاثة لاعبون، يُميَّزون بالألوان: **أبيض / أسود / أحمر** (أو أبيض/رمادي/أسود).
- يلعبون بالتناوب في اتجاه عقارب الساعة: اللاعب 1 ← اللاعب 2 ← اللاعب 3 ← اللاعب 1 ...

### 2.3 الحركة
- تتحرك القطع بنفس قواعد الشطرنج الكلاسيكي، مع تكييف الأقطار والصفوف لتعبر مركز اللوحة السداسية.
- **عند مركز اللوحة:** يستمر الصف/القطر إلى الجهة المقابلة (هذه أهم نقطة تقنية تحتاج عناية في حساب الحركات).
- يُسمح بـ: **التبييت (Castling)**، **الأخذ بالتجاوز (En passant)**، **ترقية البيدق (Promotion)**.

### 2.4 شروط الفوز والخسارة
- **كِش مَلِك (Checkmate):** اللاعب الذي يُكِشّ ملك خصمه ويأسره يربح، والخاسر يخرج من اللعبة.
- **خياران للتعامل بعد أول كِش مات** (يُختار أحدهما في الإعدادات):
  - **خيار A (الأبسط – موصى به للنسخة الأولى):** أول من يحقق كِش مات يفوز وتنتهي اللعبة فورًا.
  - **خيار B:** يُزال اللاعب المهزوم وقطعه من اللوحة (أو تتجمّد)، ويستمر اللاعبان الباقيان حتى يبقى واحد.
- **التعادل (Stalemate):** إذا لم يكن لدى اللاعب صاحب الدور أي حركة قانونية وليس في كِش، يُتخطّى دوره (في خيار B) أو يُعلَن تعادل.

> ملاحظة للمطوّر: ابدأ بالخيار A لتبسيط المنطق، ثم أضف الخيار B لاحقًا.

---

## 3. واجهة المستخدم (UI/UX)

### 3.1 المكوّنات الأساسية للشاشة
```
┌─────────────────────────────────────────────┐
│   شريط علوي: اسم اللعبة + زر "لعبة جديدة"      │
├──────────────┬──────────────────────────────┤
│              │                              │
│  لوحة معلومات │     اللوحة السداسية           │
│  اللاعبين     │     (SVG / Canvas)           │
│  (3 بطاقات)  │                              │
│              │                              │
├──────────────┴──────────────────────────────┤
│   شريط سفلي: سجل الحركات + مؤشّر دور اللاعب    │
└─────────────────────────────────────────────┘
```

### 3.2 بطاقة كل لاعب (Player Card) — ثلاث بطاقات
- اسم/لون اللاعب.
- مؤشّر "دورك الآن" يُضيء عند دور اللاعب.
- عدّاد القطع المأسورة.
- حالة: نشِط / في كِش / خرج من اللعبة.

### 3.3 اللوحة
- تُرسَم باستخدام **SVG** (مفضّل لسهولة التعامل مع الأشكال السداسية والنقر على الخانات).
- كل خانة عنصر قابل للنقر، له مُعرِّف فريد (انظر نظام الإحداثيات في القسم 4).
- **عند اختيار قطعة:**
  - تُمَيَّز الخانة المحدّدة بإطار/لون.
  - تُعرَض الحركات القانونية كنقاط/دوائر على الخانات الممكنة.
  - النقر على خانة قانونية ينفّذ الحركة.
- القطع تُعرَض إمّا برموز Unicode للشطرنج (♔♕♖♗♘♙ ...) أو صور SVG، مع تلوينها حسب اللاعب.

### 3.4 التفاعل
- لعب بالتناوب على **نفس الجهاز** (Hotseat) في النسخة الأولى.
- إشعار مرئي عند الكِش وعند انتهاء اللعبة (Modal/نافذة منبثقة).

---

## 4. التصميم التقني (Architecture)

### 4.1 تمثيل اللوحة ونظام الإحداثيات
- اللوحة السداسية لا تُمثَّل بمصفوفة 8×8 بسيطة.
- **الطريقة الموصى بها:** خريطة (Map/Object) تربط كل خانة بمُعرِّف نصّي فريد، مثل:
  ```js
  // مثال: مفتاح الخانة → بيانات
  board["a1"] = { piece: "P", owner: "white" };
  board["l8"] = null; // خانة فارغة
  ```
- يجب توثيق نظام تسمية الخانات الثلاثة قطاعات بوضوح، خاصة خانات **مركز اللوحة** حيث تلتقي القطاعات.
- بديل متقدّم: استخدام إحداثيات سداسية (axial/cube coordinates) إن أراد المطوّر دقّة هندسية أعلى.

### 4.2 نموذج البيانات (State)
```js
const gameState = {
  board: { /* خريطة الخانات */ },
  players: [
    { id: "white", name: "اللاعب 1", active: true, inCheck: false, captured: [] },
    { id: "black", name: "اللاعب 2", active: true, inCheck: false, captured: [] },
    { id: "red",   name: "اللاعب 3", active: true, inCheck: false, captured: [] },
  ],
  currentTurnIndex: 0,        // مؤشّر الدور (0,1,2)
  turnOrder: ["white","black","red"],
  selectedSquare: null,       // الخانة المحدّدة حاليًا
  legalMoves: [],             // الحركات القانونية للقطعة المحدّدة
  moveHistory: [],            // سجل الحركات
  status: "playing",          // playing | check | checkmate | finished
  winner: null,
};
```

### 4.3 وحدات الكود المقترحة (Modules)
| الوحدة | المسؤولية |
|--------|-----------|
| `board.js` | تعريف اللوحة، الإحداثيات، الجيران، علاقات المركز |
| `pieces.js` | منطق حركة كل قطعة (بيدق، حصان، فيل، رخ، وزير، ملك) |
| `rules.js` | الحركات القانونية، الكِش، الكِش مات، التبييت، الترقية |
| `game.js` | إدارة الحالة، تبديل الأدوار، شروط الفوز |
| `ui.js` | رسم اللوحة (SVG)، التعامل مع النقر، تحديث البطاقات والسجل |
| `main.js` | نقطة الدخول، ربط الوحدات |

### 4.4 الدوال الأساسية المطلوبة
```js
getLegalMoves(square)        // كل الحركات القانونية لقطعة في خانة
isMoveLegal(from, to)        // هل الحركة قانونية؟
makeMove(from, to)           // تنفيذ الحركة وتحديث الحالة
isInCheck(playerId)          // هل ملك اللاعب في كِش؟
isCheckmate(playerId)        // هل اللاعب في كِش مات؟
nextTurn()                   // الانتقال للّاعب التالي (مع تخطّي الخارجين)
renderBoard()                // إعادة رسم اللوحة بالكامل
```

> **تحدٍّ رئيسي:** حساب الحركات عبر مركز اللوحة السداسية. يُنصح بكتابة جدول/خريطة جيران (adjacency) لكل خانة في الاتجاهات الستة، وبناء حركات الرخ/الفيل/الوزير بتتبّع الاتجاه خطوة بخطوة عبر هذا الجدول.

---

## 5. هيكل الملفات المقترح
```
/chess1x3
├── index.html          # هيكل الصفحة
├── styles/
│   └── main.css        # التنسيق
├── scripts/
│   ├── board.js
│   ├── pieces.js
│   ├── rules.js
│   ├── game.js
│   ├── ui.js
│   └── main.js
├── assets/             # صور/أيقونات القطع (اختياري)
└── README.md
```

---

## 6. خطة التنفيذ المرحلية (Roadmap)

**المرحلة 1 — الأساس:**
1. إنشاء `index.html` والتخطيط العام (CSS).
2. رسم اللوحة السداسية بـ SVG مع 96 خانة قابلة للنقر.
3. بناء نظام الإحداثيات وخريطة الجيران.

**المرحلة 2 — القطع والحركة:**
4. وضع القطع في مواقعها الابتدائية.
5. تطبيق حركة كل نوع قطعة (ابدأ بالحصان والبيدق ثم القطع الخطّية).
6. معالجة الحركة عبر المركز.

**المرحلة 3 — قواعد اللعب:**
7. اختيار القطعة وإظهار الحركات القانونية والنقر للتحريك.
8. تبديل الأدوار بين الثلاثة.
9. كشف الكِش والكِش مات (خيار A: أول كِش مات يفوز).

**المرحلة 4 — اللمسات النهائية:**
10. الترقية، التبييت، الأخذ بالتجاوز.
11. سجل الحركات، عدّادات الأسر، النوافذ المنبثقة.
12. زر "لعبة جديدة" وإعادة الضبط.

**المرحلة 5 — إضافات اختيارية:**
- خيار B (إزالة اللاعب المهزوم واستمرار اللعب).
- لعب عبر الشبكة (مرحلة لاحقة، تحتاج خادمًا).
- ذكاء اصطناعي بسيط لمحاكاة لاعب.

---

## 7. معايير القبول (Acceptance Criteria)
- [ ] اللوحة السداسية تُعرَض بثلاثة قطاعات و96 خانة بألوان متبادلة صحيحة.
- [ ] القطع الثلاث (48 قطعة) في مواقعها الابتدائية الصحيحة.
- [ ] كل قطعة تتحرك وفق قواعدها، بما فيها العبور الصحيح عبر المركز.
- [ ] الأدوار تتناوب بين اللاعبين الثلاثة بالترتيب.
- [ ] يُكتشَف الكِش، ولا يُسمَح بحركة تُبقي الملك في كِش.
- [ ] تُعلَن نهاية اللعبة عند الكِش مات مع اسم الفائز.
- [ ] الواجهة تعمل في المتصفحات الحديثة دون أخطاء في الـ Console.

---

## 8. ملاحظات للمطوّر (Sonnet)
- ابدأ بالنسخة الأبسط (Hotseat على جهاز واحد + خيار A للفوز).
- أصعب جزء تقنيًا هو **هندسة اللوحة السداسية وعبور المركز** — خصّص له وقتًا وابنِ خريطة جيران واضحة وموثّقة.
- اكتب الكود معياريًا (Modules) ليسهل اختبار كل وحدة على حدة.
- يُفضَّل SVG على Canvas لسهولة التفاعل مع الخانات الفردية.
- وثّق نظام تسمية الخانات في تعليق أعلى `board.js`.
```
